# Reference Award Types

Use `GET /api/v2/references/award_types/` to fetch award-type mappings.

## Inputs
- No documented inputs.

## Output shape
- `award_types`: array of `{ label, value }` style entries.

Use these values as canonical filter tokens in search and award endpoints.
